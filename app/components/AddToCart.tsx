'use client';
import React from 'react'

const UsersPage = () => {
    return (
        <div>
            <button onClick={() => console.log('Click')}>Add to Cart</button>
        </div>
    )
}

export default UsersPage